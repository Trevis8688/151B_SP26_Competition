# GRPO v2 deps on top of scipy-ml-notebook:stable.
# Pinned to versions verified on exp_009 Colab run (CUDA 12.4 stack).
# If torch is already present and CUDA-correct, the pip install is a no-op for torch.

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
--extra-index-url https://download.pytorch.org/whl/cu124

bitsandbytes>=0.46.1
trl==0.21.0
peft
accelerate
transformers
datasets
huggingface_hub

# Judger deps (already in my-virtenv; re-install for the container)
sympy
antlr4-python3-runtime==4.11

# Flash Attention 2 — O(n) attention memory instead of O(n²).
# Critical for fitting max_completion=6144 on a5000 24GB.
# Falls back to SDPA's memory-efficient kernel if wheel isn't pre-built.
flash-attn>=2.6.0
