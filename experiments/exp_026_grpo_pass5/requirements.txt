# GRPO v2 deps on top of scipy-ml-notebook:stable.
# Run 2 v4: HF native generate (vLLM path abandoned due to Qwen3 support gap
# in vllm 0.6 + cascading optional-dep import failures in vllm 0.8).
# Reverted to torch 2.5.1 stack matching exp_009 known-good setup.

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
--extra-index-url https://download.pytorch.org/whl/cu124

bitsandbytes>=0.46.1
trl==0.21.0
peft
accelerate
transformers
datasets
huggingface_hub

# Judger deps (already in my-virtenv; re-install for the container)
sympy
antlr4-python3-runtime==4.11
